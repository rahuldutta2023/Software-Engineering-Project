# ML Models Directory

Place your trained model files here:
- crop_recommendation_topk_model.pkl
- yield_predictor.pkl

These should be in the same format as your existing models.

If you don't have these files yet, the backend will raise an error on startup.
You can train them using the scripts in the parent directory.
